# DEPRECATED: v2 is now part of the main aptos-sdk package.
# Publishing is handled by .github/workflows/publish.yaml.

name: "[DEPRECATED] Publish aptos-sdk-v2 to PyPI"

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  skip:
    name: Skipped (v2 now published as part of aptos-sdk)
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "no-op"
